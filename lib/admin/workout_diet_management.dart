import 'package:flutter/material.dart';

class WorkoutDietManagement extends StatelessWidget {
  const WorkoutDietManagement({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Workout & Diet Management")),
      body: const Center(child: Text("Feature Coming Soon...")),
    );
  }
}
