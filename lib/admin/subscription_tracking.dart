import 'package:flutter/material.dart';

class SubscriptionTracking extends StatelessWidget {
  const SubscriptionTracking({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Subscription Tracking")),
      body: const Center(child: Text("Feature Coming Soon...")),
    );
  }
}
